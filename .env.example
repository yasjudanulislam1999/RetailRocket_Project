# Copy this file to ".env" later (never commit .env)
# Example:
# cp .env.example .env

# Recommender settings (we will use these later)
TOPK=50
VIEW_WEIGHT=1.0
BUY_WEIGHT=3.0

# MLflow / DagsHub (later milestones)
MLFLOW_TRACKING_URI=
MLFLOW_TRACKING_USERNAME=
MLFLOW_TRACKING_PASSWORD=

# AWS (later milestones)
AWS_REGION=eu-west-2
S3_BUCKET=YOUR_BUCKET_NAME_HERE
S3_PREFIX=retailrocket-item2item
